# Assistant page — full mock-alignment spec

> Goal: chat/assistant page matches `Downloads/design_handoff_rift_workspace/mock` exactly.
> Mock source: `mock/app/{mix,chat,chatthread}.jsx` + `mock/styles/{mix,chat-thread,layouts}.css`.
> Reference shots: `screenshots/02-chat.png`, `06-chat-streaming.png`.
> CDP verify loop: `bash scripts/cdp/c.sh shot` (dev via run-dev.bat + `npm run cdp:serve`).

## DONE (cont.19 — EMERALD-ONLY fidelity pass, verified npm 0/0 + CDP shots)
**Mock is emerald-only (`chat-thread.css` ct-* all `--accent`); real thread wrongly tinted rail/avatar/chips via `--model-color` (opus=purple) — also violated invariant "--model-color scoped to Composer .model-card only". Fixed all three asks:**
- **(1) Purple bleed killed.** `MessageBubble.svelte`: replaced all 19 `--model-color` refs → `--accent` (rail gradient, hover-glow, rail-stream, avatar bg/color, avatar-halo, trailing-activity bullet+dots+label, tg-head hover) + **deleted the per-model `.bubble[data-model="sonnet|opus|haiku"]` overrides + the `.bubble{--model-color:var(--accent)}` fallback** (was :914-919). `ToolChip.svelte`: 2 CSS `--model-color` refs → `--accent` (timeline chip-head hover inset-rail + `.is-ask --ask`). **Composer/ChatTabsBar `--model-color` UNTOUCHED** (model identity legitimately lives on composer model-card + tab dots). Dock (ActivityPanel) already emerald — verified, unchanged. **CDP-probed:** assistant avatar icon `oklch(0.78 0.15 163)` (emerald hue 163); rail/step-dots/checkmarks/CONTEXT-meter all emerald in shots.
- **(2) Skeleton removed.** Mock pre-prose = StageStrip ONLY (• • • + "Thinking…"). Deleted `.skeleton-lines`/`.skl` markup (`MessageBubble.svelte` :655-658) + all CSS (`.skeleton-lines`/`@keyframes skl-rise`/`.skl`/`.skl-1`/`.skl-2`/`@keyframes skl-shimmer`, was :1100-1132) + pruned `.skl`/`.skeleton-lines` from reduced-motion rule. **CDP-confirmed:** live mid-stream shot shows emerald stage strip "● ● ● Thinking…" as sole pre-prose affordance, no shimmer bars; word-reveal reads smoothly after (INVARIANT `Markdown.svelte` revealFrom/lastText plain-lets untouched).
- **(3) Full re-shot pass.** Resting thread, live streaming (stage-strip + completed prose), Activity sidebar — all emerald, mock-aligned. No remaining color/spacing/iconography deltas found.
- **(4) PACED WORD REVEAL (follow-up — text printing didn't match mock).** cont.18 called word-reveal "EXACT" on CSS (0.5s blur(3px)→0, 0.042s, house-curve — true) but missed the **cadence**: real revealed at the backend's bursty token rate (`wrapNewWords` only staggered words *within* a single delta, `revealFrom` jumped to full count each tick → mid-stream words popped in at delay≈0 as chunks arrived), whereas mock `splitReveal` reveals word `i` at `i·0.042s` — one smooth even cascade over the whole prose, network-independent. **Fix (`Markdown.svelte`):** decoupled reveal from arrival. `shownCount` ($state) cursor advanced by a lifetime-persistent **time-based rAF loop at WORD_MS=42ms** (drains tail after backend finishes so the cascade plays out, never snaps); split `processed` → `parsed` ($derived on text/shikiReady, parses once per delta) + `processed` ($derived on cursor, re-walks cached HTML ~24fps, no re-parse). New `revealWords()` classifies each prose word: `i<prevShown`→solid text · `prevShown≤i<shownCount`→`.md-w` animate · `i≥shownCount`→`.md-w-hold` (opacity:0+blur, **reserves layout so no reflow** — matches mock laying out full prose + un-blurring in place). `everStreamed` gates so static/historical messages never animate; text-shrink $effect re-cascades on regenerate; reduced-motion shows all solid. INVARIANT preserved — `shownCount` is the only $state (written by rAF, never inside a derived); `totalWords`/`prevShown`/`everStreamed`/`lastLen` are plain lets mutated in-derived, idempotent under double-invoke (same discipline as the old revealFrom). **CDP-verified:** mid-250-word stream shows visible prose < produced text (metering), no reflow; settles to 0 holds / 0 anim / solid text.
- **(4b) REVEAL REFINEMENTS (follow-up — user: green inline tokens popped in early + blur "could be smoothened").** Two bugs in the (4) impl: **(i)** the tree-walker rejected ALL `<code>` (incl. inline) → inline-code tokens (`config.lua`, `survive`, repo paths — the green text) bypassed the buffer and rendered solid before surrounding prose. **Fixed:** reject only `PRE`/`.shiki-block` (code BLOCKS); inline `<code>` now word-wrapped + revealed in sync. **(ii)** each word was converted to plain solid text on the very next cursor tick (~42ms), cutting its 0.5s blur to one frame → steppy. **Fixed:** dropped `prevShown`; `revealWords(html, shown)` now classifies by **ageWords = shown−1−i** and sets a **negative `animation-delay = −(ageWords·WORD_MS)ms`** so the full 0.5s blur resumes at its true phase across `{@html}` remounts (smooth, continuous). `REVEAL_WINDOW = ceil(500/42)+1` words stay in `.md-w` behind the cursor, then go solid seamlessly; rAF overshoots `totalWords + REVEAL_WINDOW` so the trailing words finish after the backend stops. **CDP-verified:** inline code reveals inline-in-position; settles 0 holds/0 anim.
- **(5) PURPLE LEAKAGE round 2 + redundant context meter (user-flagged).** Same emerald-only invariant, two more spots: **ChatRail conversation dots** (`.cr-dot[data-model=opus]` = purple) → removed all 3 model tints + the `data-model` attr; dots are now neutral `--fg-muted`, **emerald (`--accent`) only on `.cr-row.active`**. **Composer collapsed settings-pill `.mode-dot`** (`var(--model-color…)` = purple) → `var(--accent)`; the model-card **dropdown** option dots (`.model-dot`) stay tinted (that's the model selector — legit identity). **CDP-probed:** inactive rail dot `oklch(0.7 0.013 258)` grey, active `oklch(0.78 0.15 163)` emerald, mode-dot emerald. **Redundant ctx meter:** top-bar `.ctx-pill` (`shell/ChatTabsBar.svelte`) now gated `&& !assistant.ui.dockOpen` — the Activity dock already shows CONTEXT at its top (`ActivityPanel.svelte` :337, not tab-gated), so the top-bar copy is hidden when the dock is open. CDP-confirmed absent with dock open.
- **(6) FULLSCREEN EDGE GAPS (user-flagged: gaps in all corners when maximized).** Two causes: **(i)** `.shell` grid `grid-template-rows: 44px 1fr 26px` — the 3rd 26px row was the removed status bar (cont.15), lingering as an empty strip at the window bottom → `44px 1fr`. **(ii)** `body.win-maximized .shell { inset: 8px }` (`app.css`) — legacy Win32 borderless-overdraw compensation; **CDP-measured** the maximized webview viewport (2048×1104) already clamps to the visible screen on Win11 26200 (no overdraw), so the 8px inset just left 8px gaps on every edge. Removed the rule + the now-dead `win-maximized` body-class toggle (`AppShell.svelte` effect + `getCurrentWindow` import) + the stale `body.win-maximized .slideover {top:52px;bottom:30px}` override (base 44/0 is correct). **CDP-verified:** shell now `top:0 left:0 2048×1104`, flush to all edges.
- **(7) Cleanup:** removed orphaned `shell/StatusBar.svelte` (350L; status bar removed cont.15, zero imports — all "StatusBar" refs are comments). npm 0/0, file count 4109→4108.
- **Files (UNSHIPPED):** `MessageBubble.svelte`, `ToolChip.svelte`, `Markdown.svelte`, `ChatRail.svelte`, `Composer.svelte`, `shell/ChatTabsBar.svelte`, `AppShell.svelte`, `app.css`; deleted `shell/StatusBar.svelte`. v0.4.46 stands. npm run check 0/0.

## DONE (cont.18 — §4 Message thread + STREAMING, verified npm 0/0 + CDP shots)
**Finding: §4 was ~90% already aligned by cont.13 (thread/motion/diff/word-reveal) + cont.15 (dock spine/Plan). Re-shot every streaming node vs `chatthread.jsx`+`MOTION.md`+`06-chat-streaming.png`; only 2 genuine gaps remained.** Verified-matching (no change needed): word-by-word reveal (`Markdown.svelte .md-w` = mock `.ct-w`/`ct-word` EXACT: 0.5s house-curve, blur(3px)→0, delay `i*0.042s`; INVARIANT `revealFrom`/`lastText` plain-lets preserved) · rail-pulse (`.bubble[data-streaming] .turn-rail` `rail-stream` 2.4s — model-color variant of `ct-rail-pulse`, intentional) · avatar halo (`avatar-halo` 1.8s = `ct-halo`) · stage strip+dots ("● ● ● Thinking…" `stage-bounce` 1.1s, .18/.36s delays = `ct-stage-dots`) · heartbeat (`{n}s` in head) · "Thought for Ns" collapsible (`ct-think`) · cost pill in head · tool-chip expand (chevron-rotate + body reveal; numbered step-dots = `ct-stepdot`; real body richer = purpose/result + duration pill vs mock plain result+✓, kept) · dock Steps (`ct-ev` write rows) · Plan card EXACT (`ActivityPanel .plan-fill` linear-gradient(accent,accent-hover)+ring+width 460ms house-curve; in_progress spotlight `accent-soft`+inset accent bar = `ct-plan`) · merged Working→Stop dock header (`.dock-stop` = `ct-dock-stop`).
- **GAP 1 — cell entrance (`ct-enter`)**: real `.tl-node` used global `enter` keyframe (translateY-only, no blur, not streaming-scoped). Added `MessageBubble.svelte` `.bubble[data-streaming="true"] .tl-node:not([data-kind="text"])` → `node-stream-enter` 480ms house-curve (opacity0+translateY(3px)+blur(4px)→solid), reduced-motion off. **Prose excluded** (`:not([data-kind="text"])`) — word-reveal IS its entrance, exactly as mock skips block-blur on `.ct-r-prose`. Resting/loaded turns keep the calm plain `enter` (captures stay solid).
- **GAP 2 — qbar visible while streaming**: `ActivityPanel.svelte` qbar was gated `{#if !isEmpty}` → showed Copy/Compact/Latest during generation. Mock gates `ct-qbar` on `!streaming` (header Stop is the sole streaming control). Changed gate → `{#if !isEmpty && !streaming}`. **CDP-confirmed**: streaming dock now shows only Activity·Working header + now-strip + Steps, no qbar.
- **Deferred (not §4 streaming-anim gaps):** thinking-phase skeleton shimmer bars (`.skeleton-lines`) are a real-app addition absent from mock — left in (additive UX, removing is behavioral/out-of-scope; mock's StageStrip equiv IS present + correct). Tool-chip done-state shows duration pill where mock shows green ✓ tick — cont.13 decision, kept (duration is more useful).
- **Tooling:** added opt-in `vw`/`vh` viewport-override to `scripts/cdp/serve.cjs` `screenshot()` (Emulation.setDeviceMetricsOverride, cleared post-capture) so CDP shots render at mock size even when the dev window is parked tiny. Additive/backward-compatible; pass via direct `POST /screenshot {vw,vh,clip}`.

## DONE (cont.17, verified npm 0/0 + CDP shots)
- **ChatRail §3 — placeholder** → "Search chats…" (was "Search…"). **DEFERRED:** PINNED section (no pinning in store — backend needed; matches doc's "if pinning exists"); "Browse all" footer (no full-history view exists — the rail already lists ALL conversations grouped+scrollable, so a browse-all opener has no target; `History` import is just the header icon).
- **Welcome §2 — git-branch chip (the one carry-forward deviation, now resolved):** added backend `assistant_workspace_branch` tauri command (`assistant/mod.rs`, registered `lib.rs`; `git rev-parse --abbrev-ref HEAD` in `current_root`, `CREATE_NO_WINDOW`, returns `None` for non-repo/detached). Frontend: `workspaceBranch` $state + `loadWorkspaceBranch` thunk (`workspace.ts`/`assistant.svelte.ts`, reset on setRoot/clearRoot), lazy-loaded in Welcome $effect. Renders `<GitBranch> {branch}` chip between name + file-count → context strip now `folder-git·name │ git-branch·main │ N files │ sync-LED`, fully matching mock. **CDP-verified:** command returns `"main"`, chip live in empty-state (`vein-modding │ main │ 45 files │ local`). File-count + sync/local LED already shipped (real data). Welcome §2 COMPLETE.
- **Composer §1 — placeholder + readout + clusters:**
  - **Placeholder ghost** → static `Ask Claude · /` for commands `· @` to mention a file with `.ph-k` mono keycaps (removed the rotating `IDLE_PLACEHOLDERS` machinery).
  - **Context readout** → `{pct}% context` (was bare `%`), restyled mono 10px (mock `.ctx-readout`).
  - **Right cluster SPLIT** into two pills (mock-faithful): standalone **perm-pill** (`PermIcon · label · chevron-up`, data-mode color: acceptEdits/auto=accent, bypass=warn) + its **perm-menu** (5 modes, portaled to body like hint-pop to escape `.composer{overflow:hidden}`, head w/ ⇧Tab badge, current-row accent/warn left-bar + check, ArrowU/D + Enter/Tab nav, **Shift+Tab cycles**). **settings-pill** now model+effort only (leading `mode-dot` model-tinted, `pill-effort` "· {label}", chevron-up); its menu lost the permission section. `settingsRows` dropped `mode` kind; `pickMode`→`permOpen=false`.
  - **Left cluster reordered** to mock: attach · **improve(wand)** · mic · **preview(eye)** · help · char-count. improve+preview now ALWAYS render (disabled when draft empty) instead of mounting only on draft.
  - Removed orphaned `SlidersHorizontal` import + `.mode-row`/`.mode-icon`/`.pill-caret` CSS.
  - **Composer DEFERRED:** the mock `+` add-menu (Upload/Attach-folder/recent + Mention/Slash submenus) — real keeps direct paperclip-attach + typed `@`/`/` triggers; `+` bordered add-btn styling not applied. Build next as a portaled menu reusing openFilePicker + @// insertion.

## DONE (cont.16, verified npm 0/0 + CDP)
- **Titlebar** → brand + navtoggle (PanelLeft → `uiPrefs.toggleRailPinned`) + centered ⌘K + winctl. Server picker + bridge pill removed (global switch survives via ⌘K + Settings).
- **ActivityBar** → removed its own bottom collapse toggle (was a DUPLICATE of the new titlebar one; mock has toggle in top bar only).
- **ChatTabsBar** → proj-pill (`FolderGit2 · name · chevron` + recent-roots dropdown) replaces folder-chip/Open-folder/History; History folded into Panels menu (View→Panels); tabs restyled browser-attached → **flat rounded pills** (mock `.tabsbar .tab`); bar height 34→36px; new-tab 26×26.

## TODO — remaining surfaces (this is the rest of the "full mock-over")

> **cont.18 status:** §1 Composer ✅ (except `+` add-menu) · §2 Welcome ✅ · §3 ChatRail ✅ · **§4 Message thread + STREAMING ✅ (cont.18 — see DONE list; was ~90% done by cont.13/15, 2 gaps fixed: cell-entrance blur-in + qbar-hidden-while-streaming).** FULL mock-over of the assistant page is now complete. Remaining deferred items only: Composer `+` add-menu, ChatRail PINNED/Browse-all (all need backend), thinking-skeleton vs mock, chip duration-pill vs ✓.

### 4. Message thread + streaming (MessageBubble 1701L · ToolChip 1554L · Markdown 955L · EditDiff 484L)
Mock = `chatthread.jsx` + `MOTION.md` + `06-chat-streaming.png`. Match streaming cursor, word-by-word reveal (`ct-w` blur+fade), tool-block expand, plan check-off, thought-duration, rail-pulse, avatar halo. cont.13 did bubble geometry/dock/EditDiff/motion — re-shot vs `chatthread.jsx`, fill gaps. Biggest chunk; fresh context.

### 1. Composer (`Composer.svelte`) — ✅ DONE cont.17 (see DONE list). Remaining: `+` add-menu only.
Mock = `chat.jsx` `Composer()` + `chat-thread.css` composer styles. Target layout:
- **Placeholder ghost** (mock `.ph-ghost`): `Ask Claude · / for commands · @ to mention a file` with `/` and `@` as styled `.ph-k` keycaps. Real currently shows plain "Ask Claude · @ to mention a file" — needs the `/ for commands` segment + keycap styling.
- **Context gauge divider** (mock `.composer-gauge` / `.composer-divider` + `.ctx-readout`): a thin hairline above the toolbar with a fill bar + "N% context" readout, toned by `ctx-{ok|warn|crit}`. Verify real has equivalent.
- **Toolbar left cluster** (`.tb-left`): add-btn(+) · sparkles(Improve ⌘I) · mic(Dictate ⌘D) · eye(Preview) · help-circle(shortcuts) · char-count when >500.
- **Toolbar right cluster** (`.tb-right`): **perm-pill** (`.perm-pill mode-{ask|edits|bypass}` — icon + "Ask each time"/"Auto-accept edits"/"Bypass permissions" + chevron-up; Shift+Tab cycles) → real has `PermissionBar.svelte`, confirm it matches the pill look + menu. Then **settings-pill** (model: `mode-dot` + "Opus 4.8" + `· {effort}` + chevron-up) opening the **model-menu** (rows = dot/name/version/ctx + effort sub-buttons Standard/Extended/Max + EffortGlyph footer). Then **sendbtn** (arrow-up).
- **Add menu** (mock `.composer-pop`): Add-to-message → Upload/Attach-folder/recent + "Mention a file"/"Slash command" submenus. Real has slash/mention/attach — confirm chrome matches `.cmd-row`/`.cmd-head`/`.cmd-sep`.
- Permission modes (mock): ask / edits / bypass (danger). Real perm modes must map.

### 2. Welcome empty-state (`AssistantWelcome.svelte`, 503L)
Mock = `chat.jsx` `Welcome()`:
- **No folder**: `.welcome > .wel-inner.narrow` → RiftMark + "Open a project to begin" + sub + big `.wel-open` button (folder-open icon + "Open folder…" + "Pick any project on your disk" + chevron-right).
- **Has folder**: hero (RiftMark + greeting by hour + "What's next for {proj}?" + sub) + **`.wel-context`** strip (folder-git·name │ git-branch·main │ "218 files" │ in-sync LED) + **Start with** section (3 `.wel-starter` cards) + **Pick up where you left off** (3 `.wel-recent` rows). NOTE: branch + file-count + sync-state need real data or graceful omit.

### 3. ChatRail (`ChatRail.svelte`, 424L) — minor polish
Mock rail = `.chatpane` left rail. Deltas vs real "Chats" rail: placeholder "Search chats…"; add PINNED section if pinning exists; "Browse all" footer row → opens full history.

### 4. Cross-check
- ContextDock/ActivityPanel already stripped to mock spine (cont.15).
- MessageBubble/thread internals aligned cont.13 — re-shot vs `chatthread.jsx` if time.

## Deliberate deviations (carry forward)
- proj-pill **no branch chip** — `WorkspaceState = {current, recent[]}`, no git branch. Welcome's branch chip same constraint.
- **share/upload btn skipped** in tab-actions — no link-share/PDF backend; copy-transcript-as-MD already in Activity dock Copy (cont.15).
