# rift-tauri — Changelog

> Live changelog = current version only. History via `git log -- docs/CHANGELOG.md`.

## v0.4.28-alpha — 2026-05-22 — multi-session audit sweep + onboarding scaffold + test wave A + lib.rs split

Big batch ship rolling up nine parallel agent sessions (M2-M9). M10 assistant.svelte.ts split deferred to next release (heavy rebase conflict over M2's HIGH cluster fixes; reorg with zero behavior change, no rush).

**Frontend state (M2).** [assistant.svelte.ts](src/lib/state/assistant.svelte.ts) — **#146/#234** `mutateStreaming` caches `streamingMsgIdx` and index-replaces instead of full-array map; **#147** `ensureThinkingFromEnvelope` matches by stable `startedAt` instead of `b === existing` (Svelte 5 `$state` proxies are never referentially equal); **#148** `handleTurnComplete` captures `convoId` before the microtask so tab-switch races resolve onto the original tab; **#149** `deleteConversation` now closes any tab pointing at the deleted id. Misc state hygiene #177-#185. [connection.svelte.ts](src/lib/state/connection.svelte.ts) — **#170-#172** connecting/reconnecting flag overlap fixed; **#214** autoReconnect 5-attempt cap with backoff; **#248** connection errors now reach the diag bus. [updates.svelte.ts](src/lib/state/updates.svelte.ts) — **#173** Tauri listeners exposed via `dispose()` for HMR cleanup.

**Shell + Assistant UI (M3).** AppShell + shell/* + assistant/*.svelte cleanup wave. **#159 #161 #168 #186 #210** timer-handle storage with onDestroy cleanup. **#160** Composer mention picker survives blur via rAF defer. **#162** Markdown checklist sync debounced from per-token to equality-check. **#169** dialogs.svelte.ts callbacks reset to no-op on AppShell destroy. **#174** aliveness flag consolidation. **#176** Titlebar server picker gets aria-expanded/haspopup + Escape. **#205 #206 #207 #209** ARIA fixes across HistoryDrawer/StepGroup/Composer/Markdown. **#208** ChatTabsBar drag state resets on dragcancel. **#212** ActivityBar Settings tooltip surfaces `Ctrl+,` alongside `Ctrl+9`. **#213** PageHeader stacked-opacity tone fix. **#215** StatusBar derives app_version from updates store.

**Settings + Sync + Terminal (M4-retry, after first M4 ghosted twice).** [Settings.svelte](src/lib/components/settings/Settings.svelte) — **#151** theme grid + STT lang picker get `role="radiogroup"` + `role="radio"` + `aria-checked`; **#152** srv-card invalid ARIA replaced with wrapper button + sibling Edit/Delete; **#186** diagCopied timer handle stored + cleared; **#187** loadAboutPaths lazy on `section=="about"`; **#188** loadServers guard against double-fetch; **#189** nav `aria-current`; **#190** `aria-checked` reflects stored pref not effective state; **#193** dropdowns reset on section change; **#194** out:fade duration 0 to avoid overlap on rapid nav; **#195** dynamic edit/delete aria-label per server name; **#197** Clear budget button disabled during save. [SyncPage.svelte](src/lib/components/sync/SyncPage.svelte) — **#154** to_delete_remote included in group selection helpers; **#155** Mirror confirm modal autofocus; **#157** relPathLabel basename fallback (no more leaking absolute FS paths); **#158** conflict-copy chip on `.rift-conflict.*` entries; **#202 + #260** dead scanAgeLabel deleted. [WatchedFoldersTable.svelte](src/lib/components/sync/WatchedFoldersTable.svelte) **#156** aborted flag for diag listen race. [RecentActivityCard.svelte](src/lib/components/sync/RecentActivityCard.svelte) **#199** fmtRel null guard. Terminal cluster: **#164** Terminal init/teardown race — orphan kill if `term_spawn` resolves post-teardown; **#165** SearchAddon + FitAddon explicit dispose; **#166** TerminalFindBar `$effect` cleanup; **#167** api.onResults rewired via `$effect` for tab-switch; **#211** TerminalPanel keydown `$effect` gated on `isVisible`.

**Backend sync + SFTP + diagnostics (M5).** **#89** sftp/transfer.rs streaming for large files instead of full-buffer Vec<u8>. **#96** auto_sync activity emit routed through buffered feed. **#99** flush_batch returns `(dispatched, ok, fail)` tuple so force_push_now's cache-clear gate fires only on `ok > 0`. **#224** lock_presence distinguishes SFTP error from absent lock. **#226** diag bus lag now logs. **#240** aborted_shrunk mutex-poison recovery. **#244** edit_trail distinguishes not-found from download-failed. **#246** rate-limit secondary ceiling. **#247** tracing/timing on hot paths. **#249** diag_state_pump gates on active subscribers. **#259** drift_scanner sha1 batching via FuturesUnordered.

**Backend lib + assistant + MCP + STT (M6, with fixes ported into M9's split commands/*.rs).** **#107** start_autosync samples status after slot install (sync.rs). **#109** bootstrap_list_files drops dead `_local_root` param (sftp.rs). **#115** session-lost emits only `{ session_id }` (no prompt re-broadcast). **#134** parallel `claude --version` + auth status via `tokio::join!`. **#175** STT `cancelRequested` flag separates handle role from intent. **#222** stderr_task panic surfacing. **#223** create_dir_all errors now log::warn (sftp.rs). **#241 #242** MCP bridge socket timeout + flush error handling. **#250** 9 console.debug calls stripped from stt.svelte.ts. **#258** 39 `format_push_string` clippy hits → `write!` macro in mcp_server.rs.

**Onboarding scaffold + tests (M8).** New `src/lib/components/onboarding/` w/ Welcome, SSHKeySetup, ProfileSetup, ServerAdd, ClaudeAuth, FirstSync, EmptyStateBanner — wired-in-isolation; AppShell trigger wiring TODO'd at top of [Welcome.svelte](src/lib/components/onboarding/Welcome.svelte). [docs/ONBOARDING.md](docs/ONBOARDING.md) expanded 42L → 221L: install, SmartScreen, SSH keygen + perms, workspace, server, Claude auth, first-sync, 8-mode troubleshooting. **#21 + #265 test wave A:** path_guard containment, sync_snapshot serialization round-trip, flush circuit-breaker math (vitest mirror of impl), sync-page.svelte.ts bucket display, assistant.svelte.ts usage accumulator. **86 Rust tests passing, 38 vitest tests passing, 0 ignored except path_guard's 2 platform-gated tests.**

**Infra (M7).** Tightened Tauri capabilities: **#229** `opener:default` → explicit `opener:allow-open-url` + `allow-open-path` + `allow-reveal-item-in-dir`. **#230** `core:default` → explicit `core:event:default` + `core:window:default` only. **#231** Cargo.toml version regex anchored to `[package]`. **#232** explicit `--channel win` to vpk + `Some("win")` to UpdateManager::new. **#233** .gitignore comment around Releases/. **#251** release.ps1 staging copy now globs DLLs + redistributables. **#252** GithubSource feed paginates via Link rel="next" header (per_page=50, was hitting 10-tag ceiling). **#253** release.ps1 `Read-Host` swapped for explicit `-Force` parameter for CI/no-TTY.

**Hot-file split — lib.rs (M9).** lib.rs **2118L → 298L registry-only**. Five new files under `src-tauri/src/commands/`: sync.rs (674L · 27 cmds), sftp.rs (~22 cmds), profile.rs, assistant.rs (wildcard re-export), update.rs (77L · 3 cmds). 86 unique `invoke()` names verified across 100 frontend call sites; zero command renames. Frontend invoke contract preserved. M6's #107/#109/#223 lib.rs fixes ported into the new commands/*.rs as part of the rebase.

**Verify.** `cargo check` 0 errors (1 pre-existing `private_interfaces` warning on update_service.rs::GithubSource — M7 followup). `npm run check` 4100 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS. `cargo test` 86 passed. `npm run test` 38 passed.

## v0.4.27-alpha — 2026-05-22 — Wave-3 security HIGHs + compaction UX + live tool dots

Backend security closed Wave-3 HIGHs #221 (model flag injection), #237 (log injection), #227 (Ed25519/DSA PEM scrub), #238 (Rust-side scrubUser in DiagBus). Velopack `Convert-ToAsciiSafe` + `[xml]` probe in release.ps1 catches U+00D7-class chars. Live tool dots gate on `unit.status === "neutral"`. ChatTabsBar ctx-pill tooltip + autoCompactDisabledNudge chip rewritten. Full prose in git log.

## v0.4.26-alpha — 2026-05-22 — assistant timeline UI + center-on-work-area

**Claude Code VSCode-style timeline.** Replaces the in-bubble tool-call + thinking presentation w/ a vertical timeline anchored to the existing turn-rail. Each block becomes a node w/ a status-colored bullet (hollow gray=thinking, filled green=done tool, pulsing accent=pending, red=error) and a small drop-shadow + inner highlight for depth. "Thought for Ns" lines lose the bordered `.reasoning` surface → flat single-line. Tool chips drop the card frame in collapsed state via new `variant="timeline"` prop on [ToolChip.svelte](src/lib/components/assistant/ToolChip.svelte); right-edge status pip removed since the rail bullet carries status. Agent + TodoWrite cards keep their chrome (their body IS the content). Step-N prose headers → uppercased dividers w/ trailing hairline; numbered `StepGroup` bubble dropped from the main flow (component intact, just unused). Rail recolored neutral gray (1.5px, `--fg-faint 38%`) so it reads as universal timeline rather than accent branding; streaming still glows accent and the last bullet pulses as the "current activity" beat. Content gap tightened 6 → 5px, hover lifts the bullet 1.15× for interactivity signal. [MessageBubble.svelte](src/lib/components/assistant/MessageBubble.svelte).

**Window centers on work area at launch.** [tauri.conf.json](src-tauri/tauri.conf.json) flips `visible: false` + `center: true`; [lib.rs::run](src-tauri/src/lib.rs) `setup()` calls `center_in_work_area(&main)` then `show() + set_focus()`. On Windows the helper calls `SystemParametersInfoW(SPI_GETWORKAREA)` via raw FFI (no new deps) so the window centers in the taskbar-excluded rect; non-Windows falls back to Tauri's `center: true`. Best-effort fallbacks if FFI fails.

**Verify.** `npm run check` 0/0. CDP-verified live: 14-bubble convo w/ mixed tool calls, reasoning beats, EditDiff, table, and prose renders w/ bullets on the rail, naked single-line chips, flat thinking nodes, and zero leakage onto the user side. Window-centering not yet runtime-tested live — fresh launch via `scripts/run-dev.bat` recommended.

## v0.4.25-alpha — 2026-05-22 — S136 hotfix: shell layout-collapse

**Bottom-of-window blank-zone killed.** Prod builds intermittently collapsed `.shell` to its content height, leaving the bottom of the window blank below the StatusBar despite the window being full-size. Root cause: percentage-height chain `body 100% → app.html wrapper display:contents → .shell 100%` works in dev but breaks under prod build conditions (display:contents height-resolution edge case in the bundled chunks). [AppShell.svelte](src/lib/components/AppShell.svelte) — `.shell` switched from `height: 100%` to `position: fixed; inset: 0`, sidestepping every parent-height-resolution path. SplashOverlay is also `position: fixed` so flow ordering is unaffected. [app.css](src/app.css) — `body.win-maximized` 8px padding moved onto `.shell` directly (`.win-maximized .shell { inset: 8px }`) since body padding doesn't push fixed children.

**Verify.** `npm run check` 0/0.

## v0.4.24-alpha — 2026-05-22 — S135 hotfix: empty-pane UX

**Empty-pane card replaces dead-end string.** [AssistantPane.svelte](src/lib/components/assistant/AssistantPane.svelte) — when a split pane has no tab assigned, the slot used to render only the unhelpful "No tab in this pane" string w/ no recovery action. Now renders an actionable card: `+ New chat` (primary) + `× Close pane` (ghost, only when `panes.length > 1`) + a `RECENT` quick-pick listing the top 3 conversations not already mounted in a sibling pane. Each handler focuses this pane first so `newTab`/`openTab` route through `assignFocusedPane` → land in the empty slot. Recent picks filter out convos held by sibling panes to avoid cross-pane tab-yank.

**Verify.** `npm run check` 0/0. CDP-verified: card renders w/ correct copy + 3 recents; `New chat` mints a tab into the empty pane (panes 2→2, tabs 1→2); `Close pane` collapses (panes 2→1); recent-row click opens convo here (panes 2→2, tabs 1→2). Visual snapshot via `shot-sel .pane-empty-card` confirmed.

<!-- Older entries (v0.4.23-alpha, v0.4.22-alpha) preserved in git log. -->

