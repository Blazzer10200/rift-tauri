# rift-tauri — Handoff

> Live = current session + RESUME HERE + CRITICAL DON'T-TOUCH. Older history via `git log -- docs/HANDOFF.md`. Cap ≤600 words.

## Session 2026-06-02 (cont.) — Phases 1–3 + follow-up CDP-VERIFIED end-to-end; 1 Escape fix. P4–5 remain (on main, NOT pushed/shipped)
Plan + baseline: `docs/design/redesign-gap-audit.md`. **Full CDP sweep this session confirmed every claimed-done surface renders per spec — docs were accurate, no over-claim.** `npm run check` 0/0/4108. Detail in `git log`:
- **`fff86a2` P1** — rail expand 48↔220 (CDP: pin 48↔220 + labels ✅), PageHeader (Sync/Files), PageToolbar restyle (26→58L), SyncActivityBanner oklch (0 hex leftovers), Files conflict deeplink (`syncPage.tab='conflicts'`), motion vars, orphan `settings/Settings.svelte` deleted.
- **`d34fe7f` P2** — Settings **General** (identity card + Switch + launchAtLogin/restoreSession/confirmOnQuit + Replay-first-run); Server **drift card** + **danger zone** (Forget server) + **RCON console** UI + from-scratch UDP RCON backend (`rcon/mod.rs`, `commands/rcon.rs`; pw in keychain; targets `host:bridge_port`). All CDP-verified rendering; **RCON live round-trip still UNTESTED-by-design** (needs FXServer + password — not entered this session).
- **`cb0384c` + `d5e3eef` P3** — HistoryDrawer master-detail (CDP ✅: stat strip msgs/turns/created/updated/model + AI-recap honest empty-state + transcript-preview + Open/Branch/Export; REAL fields only — tokens/cost/diff omitted-not-faked); net-new collapsible `ChatRail` (`cr-*`, CDP 219↔0). **Expand→centered modal CDP-verified — prior "needs CDP-shot" flag CLEARED.**
- **`77c8aab` fix** — Escape now closes the history-full modal (was backdrop-click only; existing Escape handler was gated on the dropdown `historyOpen`). CDP-verified.

### RESUME HERE — Phases 4 & 5 (FRONTEND-ONLY; relaunch dev: `scripts/run-dev.bat` then `npm run cdp:serve`, drive via `bash scripts/cdp/c.sh`)
- **P4 Files** — KEEP the local|remote two-pane, ADD a right detail pane. **DECISION (user): metadata-only** — status pill + size/modified meta + status-LED pips (replace the text-`▲`) + filter chips (All/Lua/Conflicts/Modified). **DEFER the Lua code-preview** (no file-read path exists; user kept frontend-only and declined a 2nd backend exception). Files: `src/lib/components/browser/{TwoPane,LocalPane,RemotePane}.svelte`. Mockup: `…/design_handoff_rift_redesign/mockup/styles/files.css`.
- **P5 Onboarding** — restyle to ob-* left-rail stepper + ObStage anims + Welcome accent picker + FirstSync animated scan + fold ProfileSetup→ServerAdd (6→5 steps); **temp-force the first-run gate to CDP-verify then RESTORE it**; remove the stale "NOT YET WIRED" comment in `Welcome.svelte`. Files: `src/lib/components/onboarding/*.svelte`; gate `src/lib/state/onboarding.svelte.ts`. Mockup: `…/mockup/styles/onboarding.css`.
- **Method**: parallel agents edit-only (avoid `.svelte-kit` race) → orchestrator runs central `npm run check` → CDP shot per surface. OKLCH only (no hex/srgb); map mockup `--gi-*`→app tokens; **NEVER add `var(--accent-h)` to components** (false audit signal — use `var(--accent)`/`--accent-soft`). Pull exact values from `mockup/styles/*.css` + `DESIGN_TOKENS.md`. Commit per phase. Stop after P5 — `/git-ship` is a separate deliberate session.

## CRITICAL DON'T-TOUCH (frontend/redesign — full backend list in `git log`/prior HANDOFF)
- **Onboarding gate (#7):** `showOnboarding` MUST keep all 4 conditions (`!dismissed && serversLoaded && servers.length===0 && defaultSshKeyExists===false`). Loosening flashes onboarding for existing users; `defaultSshKeyExists` null=unknown→don't show. P5 must temp-force then RESTORE.
- **Accent themeable via `--accent-h`** (app.css `:root` only): write `oklch(L C var(--accent-h))`; never hard-code an accent hue. Components use `var(--accent)`/`--accent-soft` (already retheme) — do NOT add `--accent-h` to them. Status LEDs (`--ok/warn/danger/info`) stay fixed.
- **IA:** `home` top-level kbd 1; Activity is a Sync tab (`syncPage.tab`), not a workspace. Kbds: home·1 chat·2 sync·3 files·4 settings·5. Home wiring real-data only (no fabricated counts).
- **AssistantPane drop handlers on `.pane` outer only** — inner overlays break the preventDefault chain (ChatRail is a flex sibling, never an ancestor of `.pane`). `tauri.conf.json dragDropEnabled:false` required for HTML5 DnD. `.shell` MUST be `position:fixed; inset:0`.
- **RCON:** pw plaintext over UDP is how FXServer RCON works — keep it in the keychain, never on disk/IPC. Targets `bridge_port` (game port), not the SSH port.
- **Versions lockstep** across `package.json`+`Cargo.toml`+`tauri.conf.json` (+`Cargo.lock`) — only at `/git-ship`, not mid-session.
