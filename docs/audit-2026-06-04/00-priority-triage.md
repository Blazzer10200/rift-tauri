# Priority Triage — Critical / High / Medium

_52 findings worth acting on first._ [← back to index](README.md)


## HIGH

| Title | Location | Fix |
|---|---|---|
| Panic on non-ASCII grep match: byte-index slice into UTF-8 string | `C:\AI Workflow\projects\rift-tauri\src-tauri\src\assistant\mcp_server.rs:302` | Use `line.char_indices().nth(200).map(\|(i, _)\| &line[..i]).unwrap_or(line)` to find the nearest safe char boundary, or use `line.chars().take(200).collect::<String>()` for a safe |
| Split pane #each keyed by index, not stable pane identity | `C:\AI Workflow\projects\rift-tauri\src\lib\components\assistant\AssistantPage.svelte:205` | Key by a stable pane identity instead of position: {#each assistant.panes as p, i (p.tabId ?? i)}. If panes can exist without a tabId, add a stable pane id field to the pane object |
| Stdout reader stalls up to 1800 s inside handle_permission_request, risking pipe-buffer deadlock | `c:\AI Workflow\projects\rift-tauri\src-tauri\src\assistant\mod.rs:2143` | Spawn a separate tokio task for the 1800 s permission wait while the reader loop continues draining stdout into a side buffer. Use an internal channel to hand the stdin control_res |

## MEDIUM

| Title | Location | Fix |
|---|---|---|
| git child process inherits MCP stdin pipe — can corrupt JSON-RPC stream | `C:\AI Workflow\projects\rift-tauri\src-tauri\src\assistant\git_local.rs:153` | Add cmd.stdin(std::process::Stdio::null()); immediately after the creation_flags block (before the cmd.output() call) so git's stdin is always /dev/null regardless of what the pare |
| Absolute paths with `..` components bypass traversal guard | `C:\AI Workflow\projects\rift-tauri\src-tauri\src\assistant\git_local.rs:72` | Add a `ParentDir` component check for absolute paths too, before the `starts_with` test. Or canonicalize both root and the joined path with `std::fs::canonicalize` (if existence is |
| validate_path absolute-path branch bypasses `..` check — workspace escape via git_diff / git_commit | `C:\AI Workflow\projects\rift-tauri\src-tauri\src\assistant\git_local.rs:72` | Add a ParentDir guard inside the absolute-path branch, mirroring the relative-path check: ```rust if p.is_absolute() {     if p.components().any(\|c\| matches!(c, std::path::Compon |
| UTF-8 line truncation at byte offset 200 can panic on multi-byte codepoints | `C:\AI Workflow\projects\rift-tauri\src-tauri\src\assistant\mcp_server.rs:302` | Use `char_indices` to find a safe boundary, or use the `unicode-segmentation` crate. Simplest fix: ```rust let truncated = if line.len() > 200 {     let mut idx = 200;     while !l |
| Blocking std::sync::mpsc::recv() on tokio worker thread inside async fn | `C:\AI Workflow\projects\rift-tauri\src-tauri\src\stt\mod.rs:388` | Wrap the blocking receive in tokio::task::spawn_blocking: `let ring = tokio::task::spawn_blocking(move \|\| cap_ready_rx.recv()).await.map_err(...)?.map_err(...)?;` This keeps the  |
| JoinHandle from rolling_window_loop task dropped without join — leaked task races with next session | `C:\AI Workflow\projects\rift-tauri\src-tauri\src\stt\mod.rs:417` | Store the JoinHandle in ActiveSession alongside rolling_cancel. In shutdown_capture (or in stt_stop_recording after shutdown_capture), abort or await the handle before draining the |
| SHA-256 integrity check silently skipped on resumed downloads | `C:\AI Workflow\projects\rift-tauri\src-tauri\src\stt\model_manager.rs:198` | On resume, hash only the newly-received bytes but that cannot reconstruct the full-file hash. The correct fix is to either (a) re-hash the on-disk partial before appending (read it |
| Auto-expand heuristic always returns 1 for any non-identical strings, making SMALL_DIFF threshold useless | `C:\AI Workflow\projects\rift-tauri\src\lib\components\assistant\EditDiff.svelte:178` | Count the actual number of changed lines from the diff chunks, or approximate with a line-level comparison: sum the lengths of all removed and added arrays from diffArrays(oldLines |
| expandedThinking keyed by unstable grouped[] index — wrong block expanded after streaming recompute | `C:\AI Workflow\projects\rift-tauri\src\lib\components\assistant\MessageBubble.svelte:683` | Key expandedThinking by a stable identity instead of a positional index. For thinking blocks, use a block-level id if one exists on the Block type, or use the `unit.key` string (e. |
| Focus outline suppressed on buttons with no replacement | `C:\AI Workflow\projects\rift-tauri\src\lib\components\dialogs\CommandPalette.svelte:390` | Replace the blanket `outline: none` with a custom ring that matches the active style, e.g. `.cp-item:focus-visible { outline: 2px solid var(--accent); outline-offset: -2px; }`. Thi |
| Unvalidated releaseUrl opened via openUrl() without scheme/host check | `C:\AI Workflow\projects\rift-tauri\src\lib\components\dialogs\UpdateDialog.svelte:64` | Before calling openUrl, assert the URL starts with 'https://github.com/': `if (!url.startsWith('https://github.com/')) return;`. Alternatively, reconstruct the URL from the validat |
| Tooltip string literal contains un-interpolated Svelte expression | `C:\AI Workflow\projects\rift-tauri\src\lib\components\shell\ChatTabsBar.svelte:484` | Use a template literal so the expression is evaluated: use:tooltip={`Open in pane ${paneIndexFor(id)}`} |
| Partial delete + unconditional state wipe in deleteAllConversations | `C:\AI Workflow\projects\rift-tauri\src\lib\state\assistant\persistence.ts:302` | Either (a) move the state-wipe inside the try block so it only runs on full success, or (b) collect per-id errors and only wipe tabs for successfully-deleted ids. Also consider Pro |
| refreshConversations silently swallows IPC errors with no user-visible signal | `C:\AI Workflow\projects\rift-tauri\src\lib\state\assistant\persistence.ts:73` | Surface persistent failures: set host.lastError = `Failed to refresh conversations: ${String(e)}` inside the catch, or re-throw and let each call-site decide. At minimum the user s |
| Whisper stop() silently no-ops during recording start-up race | `C:\AI Workflow\projects\rift-tauri\src\lib\state\stt.svelte.ts:340` | For Whisper, call `invoke('stt_stop_recording')` unconditionally (or track a separate `startInvoked` flag set synchronously in `start()` before the event arrives), so `stop()` can  |
| stdout_task panic silently zeroes all output via unwrap_or_default | `c:\AI Workflow\projects\rift-tauri\src-tauri\src\assistant\mod.rs:1789` | Mirror the stderr_task pattern: stdout_task.await.unwrap_or_else(\|e\| { log::error!("summarize stdout task panicked: {e}"); Default::default() }). Also emit a terminal 'status:err |
| stdout drain task panic silently replaced with zero defaults | `c:\AI Workflow\projects\rift-tauri\src-tauri\src\assistant\mod.rs:1789` | Mirror the stderr pattern: `stdout_task.await.unwrap_or_else(\|e\| { log::error!("summarize stdout drain task panicked: {e}"); Default::default() })`, or propagate the JoinError as |
| No URL scheme validation — file:// and javascript: URLs accepted | `c:\AI Workflow\projects\rift-tauri\src-tauri\src\browser\mod.rs:21` | After parsing, reject any scheme other than http/https: ```rust fn parse_url(raw: &str) -> Result<Url, String> {     let u = Url::parse(raw).map_err(\|e\| format!("invalid URL: {e} |
| No URL scheme allowlist — file://, javascript:, data: accepted by browser_open / browser_navigate | `c:\AI Workflow\projects\rift-tauri\src-tauri\src\commands\browser.rs:15` | Add a scheme allowlist in parse_url() (or a new validate_url() called before open/navigate): reject anything whose scheme is not http or https. Example: if !matches!(u.scheme(), "h |
| Command injection via unsanitized `path` passed through `cmd /C code <path>` | `c:\AI Workflow\projects\rift-tauri\src-tauri\src\commands\mod.rs:27` | Avoid routing through `cmd /C`. Instead, resolve `code.cmd` explicitly (e.g. search `%LOCALAPPDATA%\Programs\Microsoft VS Code\bin\code.cmd`) and invoke it directly via `Command::n |
| Child process orphaned on timeout — not killed before drop | `c:\AI Workflow\projects\rift-tauri\src-tauri\src\stt\cleanup.rs:86` | Before returning in the timeout arm, kill and reap the child: `let _ = child.kill().await; let _ = child.wait().await; return Ok(raw.to_string());`. Alternatively, wrap the whole c |
| auto-detect language path silently clobbered | `c:\AI Workflow\projects\rift-tauri\src-tauri\src\stt\whisper.rs:65` | Pass `language` directly: `params.set_language(language);`. If a hard English default is desired, document it in the param signature and remove the `None`-means-auto-detect doc com |
| Per-turn DOM parse + TreeWalker in $derived runs on every shownCount tick (~24 fps) during streaming | `c:\AI Workflow\projects\rift-tauri\src\lib\components\ assistant\Markdown.svelte:358` | Cache the TreeWalker word-split as a list of (textNode-path, words[]) tuples derived from `parsed` alone, then on each `shownCount` tick only mutate span classNames/visibility on a |
| openTab called as side-effect of right-click context menu | `c:\AI Workflow\projects\rift-tauri\src\lib\components\assistant\HistoryDrawer.svelte:39` | Remove the openTab call from onRowContext entirely. Let the context menu items themselves decide whether to open the tab (e.g. an 'Open' action inside OpenInPaneMenu). Set ctxMenu  |
| Unhandled rejection from openTab silently swallowed in onRowContext | `c:\AI Workflow\projects\rift-tauri\src\lib\components\assistant\HistoryDrawer.svelte:39` | Add a .catch() handler: `void assistant.openTab(id).then(() => { ctxMenu = {...} }).catch(err => console.error('openTab failed', err))`, or use try/catch inside an async IIFE so er |
| style attribute whitelisted globally in DOMPurify config — enables CSS injection on LLM-controlled content | `c:\AI Workflow\projects\rift-tauri\src\lib\components\assistant\Markdown.svelte:351` | Remove `"style"` from the global ALLOWED_ATTR list. Instead, post-process Shiki HTML before it enters the sanitizer (Shiki only needs `style` on `<span>` within `<pre>`), or add a  |
| askSubmitting never reset on success — submit/cancel buttons permanently stuck | `c:\AI Workflow\projects\rift-tauri\src\lib\components\assistant\ToolChip.svelte:687` | Add a finally block in both submitAskUser and cancelAskUser that resets askSubmitting = false unless askAnswered is already true. E.g.: try { await assistant.submitAskUserAnswer(.. |
| browser_show IPC rejection silently swallowed — fail-loud violation | `c:\AI Workflow\projects\rift-tauri\src\lib\components\webview\WebBrowserPage.svelte:96` | Add a .catch handler: `void invoke("browser_show").then(syncBounds).catch((e) => { error = String(e); });` — surfaces the failure in the existing error display rather than dropping |
| applyTaskCreate assigns IDs from total tasks.length, breaking applyTaskUpdate | `c:\AI Workflow\projects\rift-tauri\src\lib\state\assistant.svelte.ts:550` | Track a separate `taskCreateCount` field on TabState (initialized to 0, incremented only in applyTaskCreate) and use `String(this.taskCreateCount + 1)` as the id. Reset alongside ` |
| opener:allow-open-url wildcard reachable from AI-generated markdown links | `src-tauri/capabilities/default.json:17` | Restrict the allow list to only the domains the app genuinely needs to open: the GitHub releases page for self-update (https://github.com/**) and remove http://** entirely. If the  |
| tool_grep: no per-file size cap before full read — unbounded allocation | `src-tauri/src/assistant/mcp_server.rs:286` | After the binary probe block, stat the file and skip if its size exceeds a threshold (e.g. MAX_READ_BYTES or a dedicated MAX_GREP_FILE_BYTES constant) before calling std::fs::read. |
| stderr drain task stops reading after cap — process can deadlock on wait() | `src-tauri/src/assistant/mod.rs:1326` | Never break early from a drain loop. Either (a) keep reading to EOF but discard bytes beyond the cap (write into a fixed [u8; N] ring, or just read-and-discard after the cap), or ( |
| Leaked stdout_task and stderr_task on child.wait() error path | `src-tauri/src/assistant/mod.rs:2790` | Before the early return, abort both tasks: `stdout_task.abort(); stderr_task.abort();`. A drop-guard (e.g. a scopeguard or RAII wrapper) is more robust so other early returns added |
| API key plaintext disk fallback survives failed keychain migration | `src-tauri/src/assistant/mod.rs:677` | On successful keychain migration, zero out and overwrite the config.json field unconditionally (not only on save_config success). On migration failure, do not fall back silently —  |
| Claude subprocess spawned with --permission-mode bypassPermissions for transcript cleanup | `src-tauri/src/stt/cleanup.rs:58` | Use --permission-mode default (or omit the flag) so the spawned Claude CLI applies normal tool-permission gates. Alternatively restrict the invocation to a read-only permission set |
| pause/resume resets full timeout instead of preserving remaining time | `src/lib/components/ToastHost.svelte:38` | Track the timer start timestamp alongside the setTimeout handle in ToastStore. On pause, record `elapsed = Date.now() - startedAt`. On resume, schedule `setTimeout(dismiss, remaini |
| Undeclared `opener` causes ReferenceError at runtime | `src/lib/components/assistant/ActivityPanel.svelte:253` | Import the Tauri shell/opener plugin and declare it: `import { open as openUrl } from '@tauri-apps/plugin-shell'` (or `@tauri-apps/plugin-opener` in Tauri 2). Replace `opener.openU |
| opener is undefined — openSource always silently no-ops | `src/lib/components/assistant/ActivityPanel.svelte:253` | Add `import { openUrl } from "@tauri-apps/plugin-opener";` at the top of the script block and replace `opener.openUrl(item.value)` with the direct `openUrl(item.value)` call, remov |
| Each-block keyed by index instead of stable pane identity | `src/lib/components/assistant/AssistantPage.svelte:205` | Change `(i)` to `(p.tabId ?? i)` — or, if tabId is always set for rendered panes, simply `(p.tabId)`. This ensures a pane's DOM/component instance is destroyed and re-created when  |
| Send/Stop/Queue button has no accessible name (a11y) | `src/lib/components/assistant/Composer.svelte:1513` | Add a dynamic aria-label that mirrors the mode: `aria-label={mode === 'stop' ? 'Stop current turn' : mode === 'queue' ? 'Queue message' : 'Send message'}`. This keeps the accessibl |
| user-image-thumb button has no accessible name | `src/lib/components/assistant/MessageBubble.svelte:662` | Add aria-label="View full size image" directly on the button element, or change the img alt to a descriptive string (e.g. alt="User uploaded image — click to view full size") so th |
| submitAskUser / cancelAskUser errors swallowed silently — no user-visible feedback on IPC failure | `src/lib/components/assistant/ToolChip.svelte:683` | Introduce a local `$state askError = $state<string \| null>(null)` reactive variable. In the catch blocks, set `askError` to a short message (e.g. `"Failed to send — please retry"` |
| Enter key always confirms regardless of focused element | `src/lib/components/dialogs/Confirm.svelte:40` | Guard with a focus check: only confirm if the confirm button (or the dialog shell) holds focus. E.g. add a ref to the confirm button and check `document.activeElement === confirmBt |
| Missing return after Task/Agent block — falls through to activity label + ToolChip append | `src/lib/state/assistant.svelte.ts:606` | Add `return;` at line 606 immediately after the closing `}` of the Task/Agent block, the same way TodoWrite/TaskCreate/TaskUpdate each return early. If Task/Agent calls should also |
| applyTaskCreate generates numeric IDs incompatible with applyTodoWrite's content-keyed IDs | `src/lib/state/assistant.svelte.ts:550` | Give TaskCreate IDs the same numeric-string format as the CLI's taskId but ensure they survive a TodoWrite. Either: (a) reset tasks entirely when switching between the two APIs and |
| Six config set* mutators leave invoke() errors unhandled — state silently diverges from backend | `src/lib/state/assistant.svelte.ts:1734` | Wrap each mutator body in try/catch. On catch, surface the error via a toast or `this.lastNotice` and do NOT apply the state mutation. Pattern: `try { await invoke(...); this.field |
| Orphan boundary block left in messages when listen() rejects | `src/lib/state/assistant/compaction.ts:143` | Move the stagedBoundary push inside the try block (after the listen() call succeeds), or add a cleanup guard in the finally block: if (tab.messages.some(m => m.id === boundaryId) & |
| Compaction state machine: forceNextFirstTurn not persisted — post-restart regression sends --resume on a non-existent JSONL | `src/lib/state/assistant/compaction.ts:208` | Persist forceNextFirstTurn (or an equivalent isPostCompaction flag) in ConversationRecord and re-hydrate it in the loader. Alternatively, derive first-turn from the absence of a JS |
| assistant_set_thinking_effort / assistant_set_permission_mode registered but never invoked — dual-storage split | `src/lib/state/assistant/helpers.ts:13` | Pick one storage layer. Easiest: on settings load call `assistant_get_thinking_effort` and `assistant_get_permission_mode` to seed state (replacing localStorage reads), and on chan |
